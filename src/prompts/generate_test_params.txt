为该 MCP 工具生成最小测试参数。

工具 : {tool name}
{描述 行}

输入外观 :
```json
{schema_json}
```

所需经费：
- 返回符合计划的 JSON
- 使用最小值： 空字符串， 0, 1, [], QQ
- 保守： 避免 URL、 文件路径或特殊值， 除非需要
- 确保提供所有所需字段
- 对于数组/对象，使用最小示例(1-2 项)

只归还 JSON 物体，没有解释。
